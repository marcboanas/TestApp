<%= simple_form_for @client, :validate => true, :remote => true, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :name, :placeholder => "Client Name" %>
  <%= f.submit "Create New Client", class: "btn btn-large btn-primary", id: "newclient" %>
<% end %>